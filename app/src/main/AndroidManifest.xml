<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.CompuzoneApp">
        <activity
            android:name=".item_producto"
            android:exported="false" />
        <activity
            android:name=".activity_lista_productos"
            android:exported="false" />
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.CompuzoneApp">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".vista.RegistroProductoActivity"
            android:exported="false"
            android:theme="@style/Theme.AppCompat.Vista" />
        <activity
            android:name=".vista.ListaProductosActivity"
            android:exported="false"
            android:theme="@style/Theme.AppCompat.Vista" />
    </application>

</manifest>